name: Android Debug Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Étape 1 : Récupération du code
      - name: Checkout code
        uses: actions/checkout@v4

      # Étape 2 : Configuration du JDK
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      # Étape 3 : Configuration de l'Android SDK
      - name: Setup Android SDK
        uses: android-actions/setup-android@v2

      # Étape 4 : Acceptation des licences Android
      - name: Accept Android licenses
        run: |
          yes | sdkmanager --licenses
          sdkmanager --update

      # Étape 5 : Installation de Node.js (utile pour React Native)
      - name: Install Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      # Étape 6 : Installation des dépendances npm
      - name: Install npm dependencies
        run: npm ci

      # Étape 7 : Vérification/Création du keystore de debug
      - name: Generate debug keystore with proper alias
        run: |
          mkdir -p android/app
          keytool -genkeypair \
            -v \
            -keystore android/app/debug.keystore \
            -alias androiddebugkey \
            -storepass android \
            -keypass android \
            -keyalg RSA \
            -keysize 2048 \
            -validity 10000 \
            -dname "CN=Android Debug,O=Android,C=US" \
            -deststoretype pkcs12

      # Étape 8 : Donne les permissions à gradlew
      - name: Grant execute permission to gradlew
        run: chmod +x android/gradlew

      # Étape 9 : Compilation de l’APK debug
      - name: Build APK
        run: |
          cd android
          ./gradlew assembleDebug --no-daemon --stacktrace --info

      # Étape 10 : Téléversement de l’APK généré
      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: debug-apk
          path: android/app/build/outputs/apk/debug/*.apk
          retention-days: 7
